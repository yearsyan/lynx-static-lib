diff --git a/base/include/value/lynx_value_api.h b/base/include/value/lynx_value_api.h
index 428418c35..fbaaea206 100644
--- a/base/include/value/lynx_value_api.h
+++ b/base/include/value/lynx_value_api.h
@@ -11,7 +11,9 @@ extern "C" {
 
 #include "base/include/value/lynx_value_types.h"
 
-#ifdef _WIN32
+#if defined(LYNX_STATIC_LINK)
+#define LYNX_VALUE_EXPORT
+#elif defined(_WIN32)
 #ifdef lynx_EXPORTS
 #define LYNX_VALUE_EXPORT __declspec(dllexport)
 #else
diff --git a/base/src/BUILD.gn b/base/src/BUILD.gn
index 99624319c..4045965df 100644
--- a/base/src/BUILD.gn
+++ b/base/src/BUILD.gn
@@ -50,6 +50,9 @@ config("base_public_config") {
   if (!base_export_symbols) {
     defines += [ "BASE_NO_EXPORT" ]
   }
+  if (lynx_static_link) {
+    defines += [ "LYNX_STATIC_LINK" ]
+  }
   if (!is_debug) {
     defines += [ "NDEBUG" ]
   }
diff --git a/base/trace/native/trace_export.h b/base/trace/native/trace_export.h
index 15f7b59a2..4b087eea1 100644
--- a/base/trace/native/trace_export.h
+++ b/base/trace/native/trace_export.h
@@ -5,7 +5,9 @@
 #ifndef BASE_TRACE_NATIVE_TRACE_EXPORT_H_
 #define BASE_TRACE_NATIVE_TRACE_EXPORT_H_
 
-#ifdef _WIN32
+#if defined(LYNX_STATIC_LINK)
+#define TRACE_EXPORT
+#elif defined(_WIN32)
 #define TRACE_EXPORT __declspec(dllexport)
 #else
 #define TRACE_EXPORT __attribute__((visibility("default")))
diff --git a/clay/BUILD.gn b/clay/BUILD.gn
index 831a64cc6..61caf79b5 100644
--- a/clay/BUILD.gn
+++ b/clay/BUILD.gn
@@ -16,6 +16,10 @@ if (is_win) {
 config("config") {
   defines = feature_defines_list
 
+  if (lynx_static_link) {
+    defines += [ "LYNX_STATIC_LINK" ]
+  }
+
   include_dirs = [
     "..",
     "$root_gen_dir",
diff --git a/clay/public/clay.h b/clay/public/clay.h
index 819e352d4..f9ab21941 100644
--- a/clay/public/clay.h
+++ b/clay/public/clay.h
@@ -9,7 +9,9 @@
 #include <stddef.h>
 #include <stdint.h>
 
-#ifdef CLAY_LIBRARY_IMPLEMENTATION
+#if defined(LYNX_STATIC_LINK)
+#define CLAY_EXPORT
+#elif defined(CLAY_LIBRARY_IMPLEMENTATION)
 
 // Add visibility/export annotations when building the library.
 #ifdef _WIN32
diff --git a/config.gni b/config.gni
index b55f45570..986c0f0e7 100644
--- a/config.gni
+++ b/config.gni
@@ -91,6 +91,11 @@ declare_args() {
   # Otherwise, no symbols from this library are exported.
   lynx_export_symbols = true
 
+  # Whether Lynx is being built for static linking on Windows.
+  # Static consumers must not see __declspec(dllimport), otherwise the linker
+  # cannot pull provider objects from a normal COFF archive.
+  lynx_static_link = false
+
   # Whether to enable headless engine with host compiling.
   is_headless = false
 
diff --git a/core/BUILD.gn b/core/BUILD.gn
index a8d7820db..df3d56768 100644
--- a/core/BUILD.gn
+++ b/core/BUILD.gn
@@ -25,6 +25,9 @@ config("lynx_public_config") {
   if (!lynx_export_symbols) {
     defines += [ "LYNX_NO_EXPORT" ]
   }
+  if (lynx_static_link) {
+    defines += [ "LYNX_STATIC_LINK" ]
+  }
 
   cflags += [
     "-Wno-c99-designator",
diff --git a/devtool/base_devtool/common/BUILD.gn b/devtool/base_devtool/common/BUILD.gn
index 8683bcdb8..68685a482 100644
--- a/devtool/base_devtool/common/BUILD.gn
+++ b/devtool/base_devtool/common/BUILD.gn
@@ -13,6 +13,10 @@ source_set("base_devtool_common_sources") {
   ]
 
   include_dirs = [ "../../../third_party/debug_router/src/" ]
+  defines = []
+  if (lynx_static_link) {
+    defines += [ "LYNX_STATIC_LINK" ]
+  }
 
   deps = [ "../native:base_devtool_common_lib" ]
 }
diff --git a/devtool/base_devtool/native/base_devtool.gni b/devtool/base_devtool/native/base_devtool.gni
index 2e3b37e21..d5f8f0749 100644
--- a/devtool/base_devtool/native/base_devtool.gni
+++ b/devtool/base_devtool/native/base_devtool.gni
@@ -28,6 +28,9 @@ template("base_devtool_source_set") {
       defines = []
     }
     defines += [ "DEFINED_BASE_DEVTOOL" ]
+    if (lynx_static_link) {
+      defines += [ "LYNX_STATIC_LINK" ]
+    }
     if (!defined(deps)) {
       deps = []
     }
diff --git a/platform/embedder/BUILD.gn b/platform/embedder/BUILD.gn
index 99c322f59..326c1260e 100644
--- a/platform/embedder/BUILD.gn
+++ b/platform/embedder/BUILD.gn
@@ -11,6 +11,9 @@ config("public_api") {
     "NAPI_DISABLE_CPP_EXCEPTIONS",
     "NAPI_DISABLE_CPP_RTTI",
   ]
+  if (lynx_static_link) {
+    defines += [ "LYNX_STATIC_LINK" ]
+  }
 }
 
 config("config") {
@@ -19,6 +22,9 @@ config("config") {
     "../../base/include/value",
   ]
   defines = [ "CLAY_LIBRARY_IMPLEMENTATION" ]
+  if (lynx_static_link) {
+    defines += [ "LYNX_STATIC_LINK" ]
+  }
   if (is_win) {
     # enable dllexport for public APIs.
     defines += [ "lynx_EXPORTS" ]
diff --git a/platform/embedder/public/capi/lynx_export.h b/platform/embedder/public/capi/lynx_export.h
index c690d1030..77efa0d35 100644
--- a/platform/embedder/public/capi/lynx_export.h
+++ b/platform/embedder/public/capi/lynx_export.h
@@ -4,7 +4,9 @@
 #ifndef PLATFORM_EMBEDDER_PUBLIC_CAPI_LYNX_EXPORT_H_
 #define PLATFORM_EMBEDDER_PUBLIC_CAPI_LYNX_EXPORT_H_
 
-#ifdef _WIN32
+#if defined(LYNX_STATIC_LINK)
+#define LYNX_CAPI_EXPORT
+#elif defined(_WIN32)
 #ifndef lynx_EXPORTS
 #define LYNX_CAPI_EXPORT __declspec(dllimport)
 #else
diff --git a/third_party/napi/BUILD.gn b/third_party/napi/BUILD.gn
index 11236ac8e..7f7409276 100644
--- a/third_party/napi/BUILD.gn
+++ b/third_party/napi/BUILD.gn
@@ -21,6 +21,10 @@ config("napi_config") {
     defines += [ "USE_PRIMJS_NAPI" ]
   }
 
+  if (lynx_static_link) {
+    defines += [ "LYNX_STATIC_LINK" ]
+  }
+
   if (is_headless && is_android) {
     defines += [ "NAPI_EXTERN=__attribute__((visibility(\"hidden\")))" ]
   }
@@ -33,6 +37,9 @@ config("private_config") {
   ]
   if (is_win) {
     defines = [ "OS_WIN" ]
+    if (lynx_static_link) {
+      defines += [ "LYNX_STATIC_LINK" ]
+    }
     cflags += [ "-Wno-c99-designator" ]
   }
   include_dirs = [
diff --git a/third_party/quickjs/BUILD.gn b/third_party/quickjs/BUILD.gn
index 4fa367d00..f1fff16c5 100644
--- a/third_party/quickjs/BUILD.gn
+++ b/third_party/quickjs/BUILD.gn
@@ -41,6 +41,9 @@ config("quickjs_private_config") {
     "LYNX_DEV",
     "CONFIG_VERSION=\"2019-09-10\"",
   ]
+  if (lynx_static_link) {
+    defines += [ "LYNX_STATIC_LINK" ]
+  }
   if (is_win) {
     defines += [
       "ALLOCATE_WINDOWS",
@@ -55,6 +58,10 @@ config("quickjs_public_config") {
     "./src/src",
     "./src/src/interpreter",
   ]
+  defines = []
+  if (lynx_static_link) {
+    defines += [ "LYNX_STATIC_LINK" ]
+  }
   cflags = []
   if (is_win) {
     cflags += [ "-Wno-c99-designator" ]
diff --git a/third_party/quickjs/include/base_export.h b/third_party/quickjs/include/base_export.h
index 81216f05d..e926b91bd 100644
--- a/third_party/quickjs/include/base_export.h
+++ b/third_party/quickjs/include/base_export.h
@@ -5,7 +5,11 @@
 #ifndef SRC_INTERPRETER_QUICKJS_INCLUDE_BASE_EXPORT_H_
 #define SRC_INTERPRETER_QUICKJS_INCLUDE_BASE_EXPORT_H_
 
-#if defined(WIN32)
+#if defined(LYNX_STATIC_LINK)
+#define QJS_EXPORT
+#define QJS_EXPORT_FOR_DEVTOOL
+#define QJS_HIDE
+#elif defined(WIN32)
 #define QJS_EXPORT __declspec(dllimport)
 #define QJS_EXPORT_FOR_DEVTOOL __declspec(dllimport)
 #define QJS_HIDE
diff --git a/third_party/debug_router/src/debug_router/common/BUILD.gn b/third_party/debug_router/src/debug_router/common/BUILD.gn
--- a/third_party/debug_router/src/debug_router/common/BUILD.gn
+++ b/third_party/debug_router/src/debug_router/common/BUILD.gn
@@ -2,6 +2,8 @@
 # Licensed under the Apache License Version 2.0 that can be found in the
 # LICENSE file in the root directory of this source tree.
 
+import("../../../../../config.gni")
+
 config("debug_router_private_config") {
   visibility = [ ":*" ]
 
@@ -27,6 +29,9 @@ config("debug_router_private_config") {
     "JSON_USE_EXCEPTION=0",
     "DEBUG_ROUTER_EXPORTS",
   ]
+  if (lynx_static_link) {
+    defines += [ "LYNX_STATIC_LINK" ]
+  }
 }
 
 config("debug_router_message_impl_config") {
@@ -34,6 +39,9 @@ config("debug_router_message_impl_config") {
 
   defines = [ "ENABLE_MESSAGE_IMPL=1" ]
+  if (lynx_static_link) {
+    defines += [ "LYNX_STATIC_LINK" ]
+  }
 }
 
 source_set("debug_router_source") {
   public_configs = [ ":debug_router_message_impl_config" ]
diff --git a/third_party/debug_router/src/debug_router/common/debug_router_export.h b/third_party/debug_router/src/debug_router/common/debug_router_export.h
--- a/third_party/debug_router/src/debug_router/common/debug_router_export.h
+++ b/third_party/debug_router/src/debug_router/common/debug_router_export.h
@@ -5,7 +5,9 @@
 #ifndef DEBUGROUTER_COMMON_DEBUG_ROUTER_EXPORT_H_
 #define DEBUGROUTER_COMMON_DEBUG_ROUTER_EXPORT_H_
 
-#ifdef _WIN32
+#if defined(LYNX_STATIC_LINK)
+#define DEBUG_ROUTER_EXPORT
+#elif defined(_WIN32)
 #ifndef DEBUG_ROUTER_EXPORTS
 #define DEBUG_ROUTER_EXPORT __declspec(dllimport)
 #else
